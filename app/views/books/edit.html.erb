<html>
  <head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="books.scss">
  </head>
  <body>
    <h2>Editing Book</h2>

    <% if @book.errors.any? %>
    <div id="error_explanation">
      <h4><%= pluralize(@book.errors.count, "error") %> prohibited this book from being saved:</h4>

      <ul>
      <% @book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<%= form_for(@book, url: "/books/#{@book.id}") do |f| %>

<p>Title</p>
<%= f.text_field :title %>

<p>Body</p>
<%= f.text_area :body %>

<p><%= f.submit 'Update Book' %></p>

<% end %>

<%= link_to 'Show', book_path(@book) %><span>|</span><a href="/books">Back</a>
  </body>
</html>